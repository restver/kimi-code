# 登录链路深潜文档 + Okta SSO 登录实施 · 对话实录

> 2026-09-01。原始对话内容，仅去除界面噪音；工具调用保留命令与关键输出。
> 背景：本仓库是 MoonshotAI/kimi-code 的 fork。当天上午先按 gen-study-docs 规范写了内置 Kimi 登录链路的源码详解文档；下午用户带着另一个项目（Continue）的 Okta 登录参考代码，要求在 VS Code 插件里移植同款登录并接自有模型 API，硬约束是"上游更新尽量不冲突"。

---

### 用gen-study-docs skill 写一篇单篇深潜 的 点击 sign in with kimi account到打开浏览器，到授权，到获取模型，到夹在模型，更新UI的文档到 源码详解 文件夹下

调用 gen-study-docs skill（单篇深潜模式）。先摸底：

> **Bash**:`rg -ni "sign in with kimi" --glob '!**/node_modules/**' -l`
> ```text
> apps/vscode/webview-ui/src/components/LoginScreen.tsx
> ```

确认链路在 VS Code 插件侧（与既有 `学习文档/源码详解/01-05` 同一子系统）。沿链逐文件读源码核实：

- 点击：`LoginScreen.tsx:42-70` handleLogin → `bridge.login()`（16 分钟 RPC 超时 `bridge.ts:28`）
- 宿主：`auth.handler.ts:16-38` → `KimiAuthFacade.login`（node-sdk）→ `KimiOAuthToolkit.login`（packages/oauth）
- 浏览器：设备码流 `oauth-manager.ts:406-457`；`onDeviceCode` 回调里广播 `LoginUrl` + `openExternal`
- 授权：RFC 8628 轮询 `oauth.ts:168-211`（pending/slow_down/expired/denied），token 原子落盘 `~/.kimi-code/credentials/`
- 模型：`GET /models` → `applyManagedKimiCodeConfig` 写 config.toml 四块（providers/models/defaultModel/services）
- UI：`refresh()` → initKey 重跑 init → `resolveAppView` → 主界面 + zustand 模型下拉 + `kimi.isLoggedIn` 菜单显隐

给迷你提纲，用户经 AskUserQuestion 确认"按提纲开写（推荐）"。

实测基线（本机 Node 22 不满足 engines>=24.15，改用 nvm 的 v24.20.0 装 deps 后）：

> **Bash**:`pnpm exec vitest run packages/oauth/test/{oauth-manager,toolkit,oauth,managed-kimi-code}.test.ts`
> ```text
> Test Files  4 passed (4)
>      Tests  135 passed (135)
> ```
> **Bash**:`pnpm -C apps/vscode exec vitest run test/app-init.test.ts test/bridge-handler.test.ts`
> ```text
> Test Files  2 passed (2)
>      Tests  37 passed (37)
> ```

成文 `学习文档/源码详解/06-登录链路详解.md`（〇-七结构 + ASCII 时序图），配套改 `08-术语表.md`（新增"登录 / OAuth"组 6 词条）与 `学习文档/README.md`（文档地图一行）。自检：§ 符号清零；抽查代码坐标并修正 4 处行号误差（`postForm` 起始 59、`DEFAULT_HTTP_TIMEOUT_MS` 57、`tokenFromResponse` 29-56、`selectDefaultModel` 736）。基线 commit `b06dbe3e`。

---

### /plan：'…/continue/gui/src/components/OnboardingCard/components/OnboardingModelsAddOnTab.tsx' 我现在有另外一个项目的一种用okta 登录，然后回调回来打开vs code的方法。这个页面是登录页面。有个handleCreateAccount->ideMessenger.request("getControlPlaneSessionInfo"->const session = await authentication.getSession->然后打开浏览器等待回调，然后获取信息。然后调用models接口。获取模型。请你按照这个项目的流程，帮我在本项目也写一个登录页面，让我可以使用okta登录的方式，拿到token，然后调用我们自己的API获取models，然后组装模型，后续就用本项目的聊天等UI。 要求：架构良好，当本项目远程代码更新的时候，最好不要有冲突

读 Continue 参考文件，并行派两个 Explore agent：

- **Continue 侧结论**：仓库里没有直接的 Okta 代码，浏览器登录是 WorkOS AuthKit（Okta 由服务端代理）；链路 = GUI `auth.login(true)` → `getControlPlaneSessionInfo` → `authentication.getSession("continue", …, {createIfNone})` → 自定义 AuthenticationProvider `createSession`（PKCE S256）→ 托管页转 `vscode://` 深链回插件 → code 换 token → 会话 AES 加密存文件、密钥在 SecretStorage → 控制面拉 assistants/models → `configUpdate` 推给 GUI。
- **本项目侧结论**：v2 引擎（默认）每请求经 `resolveModelAuthMaterial` 取凭据，provider 的 `oauth: {storage:"file", key}` 走引擎自带 `KimiOAuthToolkit` 读 `<homeDir>/credentials/<key>.json`（snake_case 六字段 wire 格式）；provider `type` 是开放字符串，`openai_responses` 已注册；`FileTokenStorage`、`resolveKimiTokenStorageName` 等已从 `@moonshot-ai/kimi-code-oauth` 导出；`harness.replaceConfigSections` 提供原子多段替换；`requiresManagedProviderLogin` 只认 `managed:kimi-code`，自有 provider 的模型就位后 `useAppInit` 直达 ready——初始化逻辑零改动。

AskUserQuestion 四问，用户选定：API 为 OpenAI 风格模型列表 + **Responses 推理接口**；Okta 参数读**外部配置文件**；登录入口用**设置开关切换独立页**（现 LoginScreen 一行不改）；token 用 **SecretStorage + 回调**。

Plan agent 出完整方案。本人核验承重事实：`Methods/Events` 是常量对象、校验 Set 自动收集；`validateParams` 是 switch（漏 case 的方法会被永久拒绝）；`TokenInfoWire` 六字段；harness `replaceConfigSections`/`removeProvider`/`supportsAtomicSectionReplace` 存在；extension.ts:30 插入点可用。写计划文件后调 ExitPlanMode。

**用户拒绝并指示**：`保存plan 然后 auto mode`——计划已存 `~/.claude/plans/structured-watching-mochi.md`，直接进入自动执行。

---

### （实施，auto mode）

建六阶段任务清单后顺序执行。

**协议骨架**：`shared/bridge.ts` 三处追加（Methods +4、Events +1、validateParams +4 case）。

**okta 模块**（全新目录 `apps/vscode/src/okta/`）：

| 文件 | 职责 |
|---|---|
| `pkce.ts` | S256 challenge / verifier / state |
| `okta-config.ts` | 读 `~/.kimi-code/okta.json`，必填 issuer/clientId/apiBaseUrl，其余默认（scopes、providerName:"okta"、端口 35173-35175、10min 超时等）；mtime 记忆化 |
| `loopback.ts` | 回环回调服务器（按序试端口、state 校验、error 短路） |
| `token-store.ts` | 会话存储 + 引擎注入 + TTL/2 刷新调度 |
| `auth-provider.ts` | `AuthenticationProvider`（PKCE + asExternalUri + 换 token + refresh + 单飞） |
| `models.ts` | `fetchOktaModels` / `applyOktaProviderConfig`（纯函数、所有权合并）/ `provisionOktaModels` / `removeOktaProvider` |
| `runtime.ts` | 单例装配、激活期 catch-up、配置缺失永不抛错 |

**/btw 旁支**：用户发 `/btw apikey 就是okta的token，接的这个token不能写在文件中` 并 fork 了一个核查 agent（后被停止）。同期 token-store.ts、models.ts、auth-provider.ts、okta-config.ts 被用户/并行会话直接改写——**设计从"SecretStorage + credentials 镜像文件"转为"SecretStorage 唯一存储 + `harness.setMemoryConfig` 注入引擎内存层"**，models.ts 的 provider 条目去掉 oauth ref 只留 `apiKey: ""`，`createEngineInjector` 上移进 token-store 导出，node-sdk 并行补了 `setMemoryConfig`/`clearMemoryConfig` 一族。本人按新设计适配 runtime（激活期重注入）并核验：

> **Bash**:`rg -n "setMemoryConfig|clearMemoryConfig" packages/node-sdk/src/kimi-harness.ts`
> ```text
> 479:  async setMemoryConfig(patch: KimiConfigPatch): Promise<void>
> 484:  async clearMemoryConfig(domains: readonly string[]): Promise<void>
> ```
> v2 实现（sdk-rpc-client-v2.ts:747）注释确认：深合并进内存层、**不落盘、reload() 后保留**；klient 契约 `configTargetSchema = z.enum(['user','memory'])`，引擎 configService 三处 memory 分支——运行时路径真实成立。

**接线**：`handlers/index.ts` +2、`extension.ts` +2（`initOktaModule`）、`handlers/okta.handler.ts`（OktaLogin/OktaStatus/OktaLogout/GetAuthMode）。

**Webview**：`services/bridge.ts` +4 包装方法；新 `OktaLoginScreen.tsx`（状态机复刻 LoginScreen）、`LoginScreenGate.tsx`（按 authMode 选页的唯一切换点）、`hooks/useAuthMode.ts`；`App.tsx` 仅改 2 行（LoginScreen → LoginScreenGate）。

**测试**：`test/okta.test.ts` 25 用例（配置/PKCE/loopback 真服务器/模型映射/token store/协议四方法）。修过的问题：

> **Bash**:`pnpm run typecheck`（首轮）
> ```text
> auth-provider.ts(70,9): TS2416 … readonly AuthenticationSession[] 与可变数组不兼容
> models.ts(122,16): TS2345 string | undefined …（noUncheckedIndexedAccess）
> test/okta.test.ts(15,10): TS1484 AddressInfo 需 type-only import
> ```
> 逐个修复（getSessions 签名、`[firstModel]` 解构守卫、type-only import、mock 参数类型）；一处断言正则没算上括号 `/\(HTTP 503\): nope/`。

> **Bash**:`pnpm exec vitest run --config vitest.config.ts`（终态）
> ```text
> Test Files  17 passed (17)
>      Tests  357 passed (357)
> ```
> **Bash**:`pnpm exec vitest run packages/node-sdk/test/sdk-rpc-client-v2.test.ts`
> ```text
> Tests  42 passed (42)
> ```

**Lint**：oxlint 两个 error（spread 空 fallback）修掉，顺手清 warning（rethrow 补 `cause`、`err`→`error`、箭头加花括号），新文件 0 error。**token 落盘核查**：`rg` 确认 okta 模块内 accessToken 只出现在 `/models` 请求头、SecretStorage、`setMemoryConfig` 三处，零文件写入。

**Changeset**：`.changeset/okta-sso-login.md`（`"kimi-code": patch`，与 `prompt-resolution-durable-events.md` 等指向 apps/vscode 的先例一致）。

---

### 把这个实施总结写到'/Users/may/Desktop/Demo/Agent/kimi-code/学习文档/plan/end.md'中 /save-chat

实施总结写入 `学习文档/plan/end.md`（该空文件为用户预建；同目录 `structured-okta.md` 是用户另存的计划副本，已在总结头部注明"token 方案以 end.md 为准"）。随后执行 /save-chat 生成本档。

---

## 成果清单

| 文件 | 变更 |
|---|---|
| `学习文档/源码详解/06-登录链路详解.md` | 新增：内置 Kimi 登录全链源码详解（〇-七 + 时序图），实测基线 135+37 测试 |
| `学习文档/08-术语表.md` | 新增"登录 / OAuth"术语组 6 词条 |
| `学习文档/README.md` | 文档地图登记 06 篇 |
| `apps/vscode/shared/bridge.ts` | +10：4 Methods、1 Event、validateParams 4 case |
| `apps/vscode/src/handlers/index.ts` | +2：oktaHandlers 接线 |
| `apps/vscode/src/extension.ts` | +2：initOktaModule 调用 |
| `apps/vscode/src/handlers/okta.handler.ts` | 新增：OktaLogin/Status/Logout/GetAuthMode 四个 RPC |
| `apps/vscode/src/okta/`（7 文件） | 新增：配置/PKCE/回环/token-store/auth-provider/models/runtime |
| `apps/vscode/webview-ui/src/services/bridge.ts` | +16：四个包装方法 |
| `apps/vscode/webview-ui/src/App.tsx` | 改 2 行：LoginScreen → LoginScreenGate |
| `apps/vscode/webview-ui/src/components/{OktaLoginScreen,LoginScreenGate}.tsx`、`hooks/useAuthMode.ts` | 新增：Okta 登录界面与模式门 |
| `apps/vscode/test/okta.test.ts` | 新增：25 个单测 |
| `packages/node-sdk/src/{rpc,kimi-harness,sdk-rpc-client-v2}.ts` + 对应测试 | （并行会话）setMemoryConfig/clearMemoryConfig 一族 + 42 测试 |
| `.changeset/okta-sso-login.md` | 新增：patch 变更记录 |
| `学习文档/plan/end.md` | 新增：实施总结（用户预建空文件后写入） |

## 使用/恢复要点

- **启用 Okta 模式**：写 `~/.kimi-code/okta.json`（issuer/clientId/apiBaseUrl 必填；其余字段见 `okta-config.ts` 头部示例，均为占位值）；删除该文件即整体回落内置 Kimi 登录。
- **Okta 应用侧**：OIDC + PKCE 必选，redirect URIs 注册 `http://localhost:35173/callback`（及 35174/35175）。
- **验证命令**：`pnpm -C apps/vscode run typecheck`；`pnpm -C apps/vscode exec vitest run --config vitest.config.ts`；F5 起 Extension Development Host 走真实登录。
- 上游冲突面：5 个上游文件共约 33 行追加式改动，其余全部在 `okta` 命名空间新文件内。
- 全部变更未提交 git。
